# Has one polygon, a slope which the balls rolls down into the endpoint.
basic_polygon

# Has two slope things with a gap in between. First one that requires user
# interaction.
first_gap

# This one has a slope, and then a horizontal platform, which is too low. This
# requires a big box.
platform_too_low

# Uses revolution joints to build a bridge out of segments.
bridge

# Features a pullet, which requires the player to place weight on one side, so
# the other side stops blocking of the finish.
pulley
